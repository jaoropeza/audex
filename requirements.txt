faster-whisper==1.1.1
numpy==2.4.6
pyaudiowpatch==0.2.12.8
soundfile==0.14.0
anthropic>=0.40.0

# Web UI server
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12

# Auth
bcrypt>=4.0.0
python-jose[cryptography]>=3.3.0

# Testing
pytest>=8.0
pytest-asyncio>=0.23
anyio[trio]>=4.0

# Multi-provider adapters
openai>=1.50.0
google-generativeai>=0.8.0
httpx>=0.27.0

# Speaker diarization (optional — only needed with --diarize)
# Pinned to 3.x — version 4+ requires torch>=2.8.0
# Note: pyannote 3.x uses deprecated use_auth_token internally; a shim in main.py
# translates it to token= for compatibility with huggingface_hub 0.25+
pyannote.audio==3.4.0

# GPU support — torch and torchaudio MUST be the same version and installed
# from the same CUDA index to avoid DLL mismatches on Windows:
#   pip install torch==2.5.1 torchaudio==2.5.1 --index-url https://download.pytorch.org/whl/cu121
torch==2.5.1
torchaudio==2.5.1
